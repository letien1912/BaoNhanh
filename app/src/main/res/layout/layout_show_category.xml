<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    tools:context="com.baonhanh.ShowCategory">

    <include
        android:id="@+id/newSpaperList_toolbar"
        layout="@layout/toolbar" />

    <android.support.v4.widget.SwipeRefreshLayout
        android:id="@+id/refreshpage_swipe"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/newSpaperList_toolbar">

        <android.support.v7.widget.RecyclerView
            android:id="@+id/catelogyRecyclerView"
            android:layout_width="fill_parent"
            android:layout_height="match_parent" />

    </android.support.v4.widget.SwipeRefreshLayout>


    <include
        android:id="@+id/myProgressBar"
        layout="@layout/progress_bar_circle" />

</RelativeLayout>
