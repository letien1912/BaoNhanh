<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.baonhanh" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/logo09"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".OpeningActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ShowCategory" >
        </activity>
        <activity android:name=".ShowContentHtmlActivity" >
        </activity>
        <activity android:name=".WebselectedActivity" >
        </activity>
        <activity android:name="com.SettingPreferences.SettingActivity" >
        </activity>
        <activity android:name=".NewspapersList" >
        </activity>
    </application>

</manifest>
